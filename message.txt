🔥 REAL-TIME OBJECT DETECTION PROJECT (Python + YOLO + Webcam)

STEP 1 — Install Python

Download Python:
https://www.python.org/downloads/

IMPORTANT:
While installing, check:
✅ Add Python to PATH

Verify installation:

Windows:
python --version

Linux:
python3 --version

---

STEP 2 — Create Project Folder

Create folder:

ObjectDetectionProject

Open terminal/CMD inside that folder.

---

STEP 3 — Create Virtual Environment

Windows:
python -m venv venv

Linux/Kali:
python3 -m venv venv

---

STEP 4 — Activate Virtual Environment

Windows:
venv\Scripts\activate

Linux/Kali:
source venv/bin/activate

If successful you’ll see:

(venv)

before terminal path.

---

STEP 5 — Install Required Libraries

Run:

pip install ultralytics opencv-python

---

STEP 6 — Create Python File

Create:

main.py

---

STEP 7 — Paste This Code

from ultralytics import YOLO
import cv2

# Load YOLO model

model = YOLO("yolov8n.pt")

# Open webcam

cap = cv2.VideoCapture(0)

# Check webcam

if not cap.isOpened():
print("Error: Webcam not found")
exit()

print("Press Q to quit")

while True:
# Read webcam frame
ret, frame = cap.read()

```
if not ret:
    print("Failed to grab frame")
    break

# Run object detection
results = model(frame)

# Draw bounding boxes
annotated_frame = results[0].plot()

# Show output
cv2.imshow("Real-Time Object Detection", annotated_frame)

# Press Q to quit
if cv2.waitKey(1) & 0xFF == ord('q'):
    break
```

# Cleanup

cap.release()
cv2.destroyAllWindows()

---

STEP 8 — Run Project

Windows:
python main.py

Linux/Kali:
python3 main.py

---

STEP 9 — First Run

First time YOLO downloads model automatically:

Downloading yolov8n.pt...

Wait for completion.

---

STEP 10 — Done ✅

Laptop webcam opens with:

* Object names
* Confidence %
* Bounding boxes

Example:
person 98%
cell phone 87%
laptop 91%

Press:
Q

to quit.

---

COMMON ERRORS + FIXES

1. No module named ultralytics

Fix:
pip install ultralytics

2. Webcam not opening

Change:
cap = cv2.VideoCapture(0)

to:
cap = cv2.VideoCapture(1)

3. Black webcam screen

Close:

* Zoom
* Teams
* Browser camera tabs

---

PROJECT TITLE

AI-Powered Real-Time Object Detection using YOLOv8 and OpenCV